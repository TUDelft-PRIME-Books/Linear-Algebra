name: Deploy Book with Changed Files

on:
  push:
    branches:
    - '**'
  workflow_dispatch:

jobs: 
  call-workflow:
    uses: TeachBooks/deploy-book-workflow/.github/workflows/deploy-book.yml@nobookfolder
    secrets: inherit
    permissions:
      contents: read
      pages: write
      id-token: write
